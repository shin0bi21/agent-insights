# Product components

This directory owns Repo Automation Score components reused across pages. Run cards, activity, job/request disclosures, reports, and implementation evidence remain product-specific even when they compose primitives from `common/components`.

Page-specific setup and presentation belong under `src/pages/<Page>/components/`. Move a component here only when multiple pages use it.
