# Task: improve homepage active navigation

Improve the public homepage top bar so the desktop and mobile navigation identify the homepage section represented by the current URL. Apply the existing active visual treatment consistently and expose the active destination accessibly. The root hero URL must not mark the logo or any section link as current. Add focused DOM tests for a section route and the root route.

Follow repository guidance, use existing section-routing helpers, run focused verification, and do not create commits, pull requests, deployments, or external artifacts. Finish with the checks actually run and genuine limitations.
